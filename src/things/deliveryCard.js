import React from 'react';

const DeliveryCard = ( {delivery: {expected, actual, order_id, cost, statuss, id} }) => {
    return (
        <div className='delivery' key={id}>
            
        </div>
    )
}

export default DeliveryCard;